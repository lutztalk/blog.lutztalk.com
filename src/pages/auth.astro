---
// This page handles the GitHub OAuth callback for Decap CMS.
// The CMS will handle the OAuth flow, we just need this page to exist.
// The CMS will process the callback and redirect appropriately.
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Authenticating...</title>
</head>
<body>
  <script>
    // Let Decap CMS handle the OAuth callback
    // It will process the URL parameters and redirect to admin
    if (window.opener) {
      // If opened in popup, send message to parent
      window.opener.postMessage({ type: 'oauth-callback', url: window.location.href }, '*');
      window.close();
    } else {
      // If not in popup, redirect to admin
      window.location.href = '/admin';
    }
  </script>
</body>
</html>


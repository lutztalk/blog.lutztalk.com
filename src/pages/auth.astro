---
// OAuth callback handler for Decap CMS
// This page handles the GitHub OAuth callback
---
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Authenticating...</title>
</head>
<body>
  <script>
    // Decap CMS will handle the OAuth callback
    // Redirect back to admin after auth
    if (window.opener) {
      window.opener.postMessage('authorization:github:success', '*');
      window.close();
    } else {
      window.location.href = '/admin/';
    }
  </script>
  <p>Authenticating... Please wait.</p>
</body>
</html>

